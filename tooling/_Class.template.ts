/**
 * File description
 * 
 * @copyright 2021-2022 IntegerEleven. All rights reserved. MIT license.
 */

//  Type imports from `/deps.ts` (alphabetical)
//  import type { Type }  from "../deps.ts"

//  Feature imports from `/deps.ts` (alphabetical)
//  import { Feature }  from "../deps.ts"

//  Type imports from local files (alphabetical: files and features from files)
//  import type { Type }  from "./MyFile.ts"

//  Feature imports from local files (alphabetical: files and features from files)
//  import { Feature }  from "./MyFile.ts"

//  Constant value imports from local files (alphabetical: files and constants from files)
//  import { CONSTANTS }  from "./constants.ts"

//  Export class-specific types (alphabetical)
//  export type SomeType = string | boolean;

export class ClassName {
  //  Public Static Properties (alphabetical)

  //  Protected Static Properties (alphabetical) - Must be prefixed with an `_` (underscore)

  //  Private Static Properties (alphabetical) - Must use the private accessor `#` (octothorpe)

  //  Public Static Methods (alphabetical)

  //  Protected Static Methods (alphabetical) - Must be prefixed with an `_` (underscore)

  //  Private Static Methods (alphabetical) - Must use the private accessor `#` (octothorpe)

  //  Public Properties (alphabetical)

  //  Protected Properties (alphabetical) - Must be prefixed with an `_` (underscore)

  //  Private Properties (alphabetical) - Must use the private accessor `#` (octothorpe)

  //  Constructors - Starting with least specific arguments

  //  Interfaces Implementations (alphabetical) - starting with least implemented methods

  //  Public Method Overrides (alphabetical)

  //  Protected Method Overrides (alphabetical)

  //  Native Overrides (alphabetical)

  //  Public Methods (alphabetical)

  //  Protected Methods (alphabetical) - Must be prefixed with an `_` (underscore)

  //  Private Methods (alphabetical) - Must use the private accessor `#` (octothorpe)
}
